{
  "name": "hospitality-ai-root",
  "version": "1.0.0",
  "description": "Root workspace for HospitalityAI Predictive Staffing System",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend --legacy-peer-deps && npm install --prefix frontend --legacy-peer-deps",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "start": "concurrently \"npm run start:backend\" \"npm run dev:frontend\"",
    "start:backend": "node backend/server.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "lucide-react": "^1.21.0"
  }
}
